numpy
shapely
tqdm

# No spconv dependency -- the sparse 3D backbone is a pure-PyTorch
# implementation (sparse_conv_pure.py), specifically to avoid spconv's own
# CUDA-tag-matching install fragility (see model.py's docstring). torch
# itself is assumed already installed (Colab ships one; for a local machine
# see README.md's Local GPU setup section for the CUDA-matched pip index).
